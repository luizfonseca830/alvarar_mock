pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp *.html public/
    - cp *.pdf public/ 2>/dev/null || true
    - cp *.png public/ 2>/dev/null || true
  artifacts:
    paths:
      - public
  only:
    - main
